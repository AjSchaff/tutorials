<odoo>
  <record id="estate_property_search_view" model="ir.ui.view">
    <field name="name">estate.property.view.search</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <search>
        <field name="name" string="Title" filter_domain="[('name', 'ilike', self)]"/>
        <field name="postcode" />
        <field name="expected_price" />
        <field name="bedrooms" />
        <field name="facades" />
        <field name="state" invisible="1"/>
        <separator />
        <group string="Filter">
          <filter name="available" string="Available" domain="[('state', '=', 'new')]"/>
          <filter name="New Properties" string="New Properties" domain="[('state', '=', 'new')]"/>
          <filter name="Offer Received" string="Offer Received" domain="[('state', '=', 'received')]"/>
        </group>
        <separator />
        <group string="Group By">
          <filter name="group_by_postcode" string="Postcode" context="{'group_by': 'postcode'}" />
        </group>
        <field name="living_area" string="Living Area(sqm)" filter_domain="[('living_area', '>=', self)]"/>
      </search>
    </field>
  </record>

  <record id="estate_property_form_view" model="ir.ui.view">
    <field name="name">estate.property.view.form</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <form string="Estate Property">
        <header>
          <button name="action_sold" type="object" class="btn-primary" string="Sold"/>
          <button name="action_cancel" type="object" class="btn-danger" string="Cancel"/>
          <field name="state" string="Status" widget="statusbar" statusbar_visible="new,received,accepted,sold"/>
        </header>
        <sheet>
          <group>
            <h1 string="Name">
              <field name="name" />
              <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
            </h1>
          </group>

          <group>
            <group>
              <field name="property_type_id" />
              <field name="postcode" />
              <field name="date_availability" string="Available From"/>
            </group>
            <group>
              <field name="expected_price" />
              <field name="best_price" string="Best Offer"/>
              <field name="selling_price" />
            </group>
          </group>
          <notebook>
            <page string="Description">
              <group>
                <field name="description" string="Description"/>
              </group>
              <group>
                <field name="bedrooms" string="Bedrooms"/>
                <field name="living_area" string="Living Area(sqm)"/>
                <field name="facades" string="Facades"/>
                <field name="garage" string="Garage"/>
                <field name="garden" string="Garden"/>
                <field name="garden_area" string="Garden Area(sqm)"/>
                <field name="garden_orientation" string="Garden Orientation" widget="radio" />
                <field name="total_area" string="Total Area(sqm)"/>
              </group>
            </page>
            <page string="Offers">
              <group>
                <field name="offer_ids" widget="one2many" options="{'no_create': True, 'no_quick_create': True, 'no_create_edit': True}" editable="False" />
              </group>
            </page>
            <page string="Other Info">
              <group>
                <field name="user_id" string="Salesperson"/>
                <field name="buyer_id" string="Buyer"/>
              </group>
            </page>
          </notebook>
        </sheet>
      </form>
    </field>
  </record>

  <record id="estate_property_list_view" model="ir.ui.view">
    <field name="name">estate.property.view.list</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <list string="Estate Properties">
        <field name="name" string="Title"/>
        <field name="property_type_id" />
        <field name="postcode" />
        <field name="bedrooms" />
        <field name="living_area" string="Living Area(sqm)"/>
        <field name="expected_price" />
        <field name="selling_price" />
        <field name="date_availability" />
      </list>
    </field>
  </record>

  <record id="estate_property_action" model="ir.actions.act_window">
    <field name="name">Properties</field>
    <field name="res_model">estate.property</field>
    <field name="view_mode">list,form</field>
    <field name="context">{'search_default_available': 1}</field>
  </record>
</odoo>